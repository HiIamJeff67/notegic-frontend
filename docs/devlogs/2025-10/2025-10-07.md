# Development log — 2025-10-07

This snapshot is generated from 1 commit dated 2025-10-07 in Git ref `origin/main`. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `aa75212` 2025-10-07 — feat: complete save material feature & download markdown feature & refactor loading provider & fix navigate to other materials cause sidebar collasped & refactor isNavigating in useAppRouter by useTransition

## Changed areas

- `src` — 25 changed-file references
- `shared` — 6 changed-file references

## Regeneration

Run `npm run devlog` or `node scripts/devlog.mjs` from the repository root. The snapshot reads local Git history and does not call GitHub or an AI service.
