# Development log — 2025-10-20

This snapshot is generated from 1 commit dated 2025-10-20 in Git ref `origin/main`. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `1da9a19` 2025-10-20 — refactor: modify the api related functions and hooks for csrf token and authorization header field

## Changed areas

- `shared` — 16 changed-file references
- `src` — 14 changed-file references

## Regeneration

Run `npm run devlog` or `node scripts/devlog.mjs` from the repository root. The snapshot reads local Git history and does not call GitHub or an AI service.
