# Development log — 2025-10-28

This snapshot is generated from 1 commit dated 2025-10-28 in Git ref `origin/main`. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `1f8370e` 2025-10-28 — feat: complete material path in editors

## Changed areas

- `src` — 9 changed-file references
- `shared` — 7 changed-file references
- `package-lock.json` — 1 changed-file references

## Regeneration

Run `npm run devlog` or `node scripts/devlog.mjs` from the repository root. The snapshot reads local Git history and does not call GitHub or an AI service.
