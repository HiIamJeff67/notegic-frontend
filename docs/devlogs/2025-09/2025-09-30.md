# Development log — 2025-09-30

This snapshot is generated from 1 commit dated 2025-09-30 in Git ref `origin/main`. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `c8abca5` 2025-09-30 — fix: fix rename shelf feature & fix move sub shelf feature including cross root shelf movement

## Changed areas

- `shared` — 11 changed-file references
- `src` — 9 changed-file references
- `package-lock.json` — 1 changed-file references
- `package.json` — 1 changed-file references

## Regeneration

Run `npm run devlog` or `node scripts/devlog.mjs` from the repository root. The snapshot reads local Git history and does not call GitHub or an AI service.
