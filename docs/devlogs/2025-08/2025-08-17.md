# Development log — 2025-08-17

This snapshot is generated from 1 commit dated 2025-08-17 in Git ref `origin/main`. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `9c9e2d0` 2025-08-17 — refactor: change the lib to util for actual lib in shared folder

## Changed areas

- `src` — 34 changed-file references

## Regeneration

Run `npm run devlog` or `node scripts/devlog.mjs` from the repository root. The snapshot reads local Git history and does not call GitHub or an AI service.
