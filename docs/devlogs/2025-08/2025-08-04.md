# Development log — 2025-08-04

This snapshot is generated from 1 commit dated 2025-08-04 in Git ref `origin/main`. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `bef414a` 2025-08-04 — feat: forget password page & api & rebuild the exception handling from backend

## Changed areas

- `src` — 18 changed-file references
- `package-lock.json` — 1 changed-file references
- `package.json` — 1 changed-file references

## Regeneration

Run `npm run devlog` or `node scripts/devlog.mjs` from the repository root. The snapshot reads local Git history and does not call GitHub or an AI service.
