# Development log — 2026-02-20

This snapshot is generated from 1 commit dated 2026-02-20 in Git ref `origin/main`. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `af5fc87` 2026-02-20 — feat: build item path & block pack editor

## Changed areas

- `shared` — 10 changed-file references
- `src` — 10 changed-file references
- `.vscode` — 1 changed-file references

## Regeneration

Run `npm run devlog` or `node scripts/devlog.mjs` from the repository root. The snapshot reads local Git history and does not call GitHub or an AI service.
