# Development log — 2026-02-23

This snapshot is generated from 1 commit dated 2026-02-23 in Git ref `origin/main`. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `a5554f7` 2026-02-23 — feat: continue on building block pack editor and block event provider with event handling flow chart & build modal for popups and dialogs components & move all manipulators to lib

## Changed areas

- `shared` — 26 changed-file references
- `src` — 25 changed-file references
- `docs` — 2 changed-file references
- `.gitignore` — 1 changed-file references

## Regeneration

Run `npm run devlog` or `node scripts/devlog.mjs` from the repository root. The snapshot reads local Git history and does not call GitHub or an AI service.
