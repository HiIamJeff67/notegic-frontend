# Development log — 2026-05-26

This snapshot is generated from 1 commit dated 2026-05-26 in Git ref `origin/main`. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `0eb0bb7` 2026-05-26 — feat: add toggle group and button group & refactor material viewer to handle all the material content types & fix app router refresh method

## Changed areas

- `src` — 20 changed-file references
- `shared` — 2 changed-file references
- `components.json` — 1 changed-file references
- `package-lock.json` — 1 changed-file references
- `package.json` — 1 changed-file references

## Regeneration

Run `npm run devlog` or `node scripts/devlog.mjs` from the repository root. The snapshot reads local Git history and does not call GitHub or an AI service.
