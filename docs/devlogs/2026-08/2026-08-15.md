# Development log — 2026-08-15

This snapshot is generated from 1 commit dated 2026-08-15 in Git ref `origin/main`. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `73f1826` 2026-08-15 — feat(settings): add API key management and improve mobile settings UX

## Changed areas

- `src` — 13 changed-file references
- `shared` — 6 changed-file references

## Regeneration

Run `npm run devlog` or `node scripts/devlog.mjs` from the repository root. The snapshot reads local Git history and does not call GitHub or an AI service.
