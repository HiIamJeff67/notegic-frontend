# Development log — 2026-08-20

This snapshot is generated from 1 commit dated 2026-08-20 in Git ref `origin/main`. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `7f72139` 2026-08-20 — feat(preferences): sync user settings and enforce local policies

## Changed areas

- `shared` — 13 changed-file references
- `src` — 13 changed-file references
- `test` — 2 changed-file references

## Regeneration

Run `npm run devlog` or `node scripts/devlog.mjs` from the repository root. The snapshot reads local Git history and does not call GitHub or an AI service.
