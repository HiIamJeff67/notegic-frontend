# Development log — 2026-07-18

This snapshot is generated from 1 commit dated 2026-07-18 in Git ref `origin/main`. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `29e925c` 2026-07-18 — refactor: retire legacy block cache and tune yjs collaboration

## Changed areas

- `shared` — 21 changed-file references
- `.nvmrc` — 1 changed-file references
- `src` — 1 changed-file references

## Regeneration

Run `npm run devlog` or `node scripts/devlog.mjs` from the repository root. The snapshot reads local Git history and does not call GitHub or an AI service.
