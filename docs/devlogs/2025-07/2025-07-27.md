# Development log — 2025-07-27

This snapshot is generated from 1 commit dated 2025-07-27 in Git ref `origin/main`. It is a deterministic repository summary; detailed intent belongs in commit messages and design documents.

## Recent commits

- `0e79dfd` 2025-07-27 — feat: using ShadCN component instead of build our own component & error handling on register and login

## Changed areas

- `src` — 42 changed-file references
- `package-lock.json` — 1 changed-file references
- `package.json` — 1 changed-file references

## Regeneration

Run `npm run devlog` or `node scripts/devlog.mjs` from the repository root. The snapshot reads local Git history and does not call GitHub or an AI service.
